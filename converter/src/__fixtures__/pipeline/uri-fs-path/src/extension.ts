import * as vscode from 'vscode'

export function activate() {
  const p1 = document.uri.fsPath
  const p2 = doc.uri.fsPath
  const p3 = workspaceFolders[0].uri.fsPath
  const p4 = arr[i].uri.fsPath
}
